import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'co.edu.iudigital.agro',
  appName: 'agro-app',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
